main() {
  bool activado = true;
  print(activado);
  // activado = !activado;
  // La sintaxis es la misma que en c o en java.
  if (!activado) {
    print('El motor esta funcionando');
  } else {
    print('Esta apagado');
  }
}
